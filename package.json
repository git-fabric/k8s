{
  "name": "@git-fabric/k8s",
  "version": "0.1.0",
  "description": "Kubernetes operations fabric app — cluster, pods, deployments, services, and logs as a composable MCP layer",
  "author": "git-fabric",
  "license": "MIT",
  "repository": { "type": "git", "url": "https://github.com/git-fabric/k8s.git" },
  "keywords": ["kubernetes", "k8s", "mcp", "model-context-protocol", "git-fabric"],
  "type": "module",
  "bin": { "fabric-k8s": "./bin/cli.js" },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["bin/", "dist/"],
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "lint": "eslint src/",
    "test": "vitest"
  },
  "dependencies": {
    "@kubernetes/client-node": "^0.21.0",
    "@modelcontextprotocol/sdk": "^1.26.0",
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "vitest": "^4.0.18"
  },
  "engines": { "node": ">=20.0.0" }
}
